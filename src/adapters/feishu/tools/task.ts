/**
 * Task Tools
 *
 * 飞书任务工具
 */

import type { PlatformTool } from "../../../core/platform/tools/types.js";
import type { LarkClient } from "../client/index.js";

/**
 * 创建任务工具
 */
export function createTaskTools(larkClient: LarkClient): PlatformTool[] {
	// 暂时返回空数组，将在后续实现
	return [];
}
