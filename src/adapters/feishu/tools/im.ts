/**
 * IM Tools
 *
 * 飞书 IM 工具
 */

import type { PlatformTool } from "../../../core/platform/tools/types.js";
import type { LarkClient } from "../client/index.js";

/**
 * 创建 IM 工具
 */
export function createIMTools(larkClient: LarkClient): PlatformTool[] {
	// 暂时返回空数组，将在后续实现
	return [];
}
