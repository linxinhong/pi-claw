/**
 * Calendar Tools
 *
 * 飞书日历工具
 */

import type { PlatformTool } from "../../../core/platform/tools/types.js";
import type { LarkClient } from "../client/index.js";

/**
 * 创建日历工具
 */
export function createCalendarTools(larkClient: LarkClient): PlatformTool[] {
	// 暂时返回空数组，将在后续实现
	return [];
}
